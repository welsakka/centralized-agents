# Code Patterns

> Recurring patterns used across this codebase. When implementing new features, follow these exactly — they encode decisions that are already made. Deviating requires an ADR.
>
> <!-- Entries are added by Docs Manager after each feature cycle. Do not add entries manually. Follow the format defined in docs/knowledge/_schema.md. -->

---

*(no entries yet)*
